# Orglet implementation status

## Starting point

The workspace contains only `plans/orglet_mvp_plan_vi.md` and its coding starter. There is no application, repository metadata, dependency manifest, or test suite. Those plans are the product contract.

## Current milestone

**0.2.0 (Windows):** M0–M4 foundation plus Grok (xAI) API and Cursor Agent local harness. Live API acceptance, clean-machine installer and code signing remain gated on user inputs (see [windows-release-gates.md](windows-release-gates.md) and [release-review.md](release-review.md)). macOS/Linux packaging is deferred.

## Decisions

- Electron Forge, React, TypeScript, Vite, Tailwind and editable shadcn-style primitives.
- Node's bundled SQLite avoids a second native ABI. Check the actual engine at startup and refuse unsafe versions.
- Business state lives in SQLite owned by the core, not React/localStorage.
- Main owns dialogs and OS-backed credential encryption. No stored secret is returned to the renderer.
- Built-in readers only. No shell or execution of imported code.
- Explicit demo mode, isolated from paid usage. No implicit mock fallback.
- Versioned workers/skills and run snapshots; permissions checked at each source read.
- Immutable usage entries in integer micro-USD; uncertain requests retain their reservation.
- Custom model IDs: fetch lists from each provider's native API or CLI, cache in SQLite, fail-open to a typed ID, never scrape HTML ([docs/model-list-fetch.md](model-list-fetch.md); COD-31 fetch+cache, COD-28 picker, and COD-30 deprecated chip).

## Validation completed locally

- Latest finding milestone: 90 tests across 14 files and strict typecheck passed. Core-assigned provenance, checker scope validation, backup tamper rejection, Markdown preservation and legacy exported skill-schema compatibility are covered. New findings carry category, recommendation and checker IDs; old reports remain readable without invented IDs.
- Packaged finding E2E passed exact source/checker focus, automatic eligible text preview, checker expansion, provenance and narrow layout. Native computer use separately observed the two-line source preview and linked CSV checker with 1 row and 2 columns. This UI report is a labelled synthetic fixture. General desktop smoke passed with persisted artifact and no renderer errors. Forge ZIP/Setup make completed; test window/process closed. See docs/finding-ui-review.md.

- Structured run logs: eight tests cover score/failure handling, within-solution sample deviation, insufficient repeats, ties/lower-is-better ranks, 15→1 = +14, malformed logs, actual CSV/JSONL DuckDB parsing, permission/backup preservation and the model tool's Markdown provenance. Full suite: 83 tests across 13 files; strict typecheck passed.
- Final packaged run-audit smoke passed: 61 rows, 60 completed, one failed score excluded, zero within-group sample deviation and +14 rank gain. Invalid-column input shows a repair message and saves no partial profile. Direction/multiple-selection controls, failure disclosure, rank table and narrow layout passed. Desktop smoke passed after the changes. Native computer use ran the checker, opened its result, then on the final build opened the rank disclosure and visibly confirmed s14: public 15, private 1, +14. Final ZIP/Setup make completed; all test windows/processes closed.

- Agent Skills: nine new integration tests cover YAML/frontmatter rejection, byte/hash integrity, path/size boundaries, review enforcement at assignment and direct runtime entry, scoped resource reads, unsupported manifests, backup/template approval isolation and native directory IO. At the skill milestone the suite passed 75 tests across 12 files.
- Packaged skill E2E passes import-error visibility, import/review/export, blocked Bash capability, local review statuses, horizontal checkbox layout and narrow overflow checks. Native computer use separately inspected the script as text, observed disabled activation, imported the exported package through the real Windows directory dialog and confirmed review. It found duplicate-name ambiguity and a misplaced checkbox; both were corrected and covered by the final packaged E2E. See `docs/skill-ui-review.md`.
- Latest checks after the skill changes: strict typecheck, full suite, desktop smoke and packaged checker/template/backup smoke passed. Forge make completed ZIP and Squirrel Setup. No provider calls or installer execution occurred.

- TypeScript strict mode, including unused local/parameter checks: passed.
- 90 integration tests passed in the latest full suite. Covers report persistence, revision snapshots, tool deny, source scope/change/size, provider-specific consent, fabricated references, unknown usage, cancellation, live revocation, interrupted recovery, shared reservations, duplicate settlement, source preview, both official SDKs against HTTP/SSE fixtures, team concurrency/join/partial retry, native dataset profiling/intake/cancellation/provenance, backup/restore, checkpoint recovery, mandatory configured preflight team template import/export, routines and shifts.
- Windows Electron smoke: visible native window, sandbox/context isolation on and Node integration off, SQLite 3.53.4, one artifact retained after restart, no renderer exceptions.
- Extended desktop flow passed: IME and Shift+Enter, selected-source preview and revocation, Markdown export, fake keys for both providers encrypted by DPAPI, drawer focus restoration, dark mode, 200% zoom, team template creation, correct team editor routing, three committed role reports and member-report expansion. `test-results/` contains captures and smoke JSON.
- Native computer use on the packaged executable: created a worker, ran a demo, accepted its report; then created Research Review from its template, ran both members and the synthesizer, inspected the activity panel and expanded a member report. These actions used Windows UI Automation and screenshots through the computer-use skill, separate from Playwright E2E.
- Dataset verification: actual Parquet fixture via DuckDB, CSV quoting and reordered/duplicate/null IDs, JSONL missing fields, malformed input rejection. Desktop E2E passes the local checker flow. A separate packaged smoke executes the shipped addon in isolated user data and returns 3 rows / 1 duplicate ID. Computer use then clicked Run and opened the same correct result in the packaged UI.
- Computer use caught missing native dependencies in the first package. Forge/Vite ignored node_modules; the packager now explicitly includes the external DuckDB dependency tree and unpacks native files. A separate fixture had mixed line endings and was corrected; strict CSV rejection was not disabled.
- Backup/restore: packaged E2E exported a report plus checker result and restored both into fresh user data; source permission was revoked and credentials stayed absent. Native computer use then selected the backup through the real Windows file dialog, reviewed the confirmation, restored it again and observed the success status. The optional test-window close handler initially accessed Playwright's disposed process; it now tracks the close event.
- Checkpoints: tests pause after a committed read, reopen SQLite and finish the same run without repeating the first paid step; resume consumes a persisted reply without provider dispatch; uncertain dispatch blocks resume with its reservation retained. Additional cases cover atomic reservation/journal rollback, source modification during pause, frozen undispatched team roles and v1→v2 migration/newer-version refusal. Electron E2E pauses, restarts and resumes a demo with the same run ID. Native computer use clicks Resume in the packaged app and observes its completed report.
- The extended E2E exposed a pre-existing narrow-layout issue: at 200% zoom, sidebar history selection left the sidebar covering task actions. Opening a task or starting a new task now collapses narrow navigation; history selection moves focus into the main content. The complete desktop smoke passed afterward.
- Preflight tests prove that configured checks finish before any role dispatch, cancellation sends no model request, a paused preflight reuses saved results after restart, changed bytes block model dispatch, and text-only/failed checks remain insufficient/partial. Tests also cover exported/restored checker provenance, model reports with 30 limitations plus app notices, and omission of excluded source names from provider context.
- Packaged E2E creates Eris Review through the UI, edits its ID-column/preflight settings, runs an automatic checker and all four demo roles, then backs up and restores the preflight/results. Native computer use opens the saved preflight, expands the result and observes 3 rows, 2 columns and 1 duplicate ID. No model API was called.
- Native inspection found a long-content grid overflow hiding the lower sidebar/main footer. Grid rows now shrink within the viewport and the main pane has a zero minimum height. Packaged E2E confirms main bottom 781 <= viewport 781 and sidebar footer bottom 769. Demo copy distinguishes actual local checker results from the sample model report.
- Template tests cover roundtrip configuration, fresh IDs, shared skills, one worker in both roles, duplicate/missing/orphan keys, unknown fields and invalid/oversized JSON. Packaged E2E exports/imports a team and verifies no task is created. Native computer use saves a template through the real Windows Save dialog and imports it through Open; the new team appears in the sidebar.
- Hardening tests prove a lowered team budget applies before the next reservation, a partial team without a synthesis artifact cannot be accepted, and resumed preflight clears stale transient error notices. Desktop E2E selects two folders and preserves both exclusion lists across restart.
- Routines: real-clock packaged E2E creates an approved schedule and observes its automatic demo, then restarts with a 30-day overdue fixture and verifies a reopen notice, one coalesced missed-run explanation, and one explicit catch-up. It also checks missing-approval/invalid-timezone feedback, disabling schedules, team shift/concurrency settings, a generated handoff, outside-shift resume rejection and narrow layout. Native computer use changes daily to weekly, observes approval reset, saves it, disables it and opens/closes the handoff disclosure. The invalid-timezone error initially lacked a repair path; it now names the field and gives examples, with inline error association and focus.
- Core tests cover timezone/DST gaps and overlaps, overnight shifts, exact occurrence/task commit, first-tick/gap/overdue deferral, long-overdue reopen with one preserved `pending.dueAt`, dismiss-without-moving-next-due, offline coalescing, duplicate catch-up, source/config changes, disable-during-verification races, restored schedules without approval, failed-write rollback, budget refusal, live capacity, step-boundary pauses and deterministic handoff recovery. Model/pricing catalog changes also invalidate recurring approval.
- Forge produced the portable Windows package, ZIP and Squirrel Setup. Installer execution remains untested. Artifacts are unsigned.

## Validation still required

- Live provider credentials: the user said they will supply an API key later and instructed continuing the API integration now. A single future OpenAI acceptance task is authorized up to $0.05 once the user supplies the key path. No real provider request has been made.
- Installer execution on this Windows machine, clean-machine installation and signing are distinct checks.
- M2 has folder intake, persisted exclusion notices, deterministic CSV/JSONL/Parquet profiling and a template-configured preflight before all model roles. New Eris templates enable it; existing teams retain their configuration. Preflight is generic policy, not a branch on the team name. Still needed: richer scoring/stability checks, line-level citations, related findings/conflict preservation, structured overall recommendation/check coverage and an actual Eris benchmark.
- M3: OpenAI and Anthropic are wired through SDK adapters, main-process encrypted credentials, per-provider consent and pricing. Codex remains disabled pending conformance; do not imply subscription integration exists.
- M4: additive backup/restore, cooperative pause, durable checkpoint/resume, step attempts, startup lease recovery, heartbeat records, per-run event sequences and team JSON template transfer are implemented. Schema v2 adds runtime tables; v3 adds preflight records; v4 adds routines. Newer database versions are refused. Current settings remain unchanged during restore; imported sources/tasks receive no read/provider consent, and checkpoint context is excluded. Daily/weekly routines, one explicit missed-run catch-up, team work hours, concurrency limits and deterministic shift handoff are implemented. Restored routines are disabled and lose approval. Agent Skills directory import/review/export is implemented; rollback tooling remains open.
- M5: real benchmark fixtures, live provider acceptance, clean-machine installer testing and release checks remain open. The full goal is not complete.

## Continuation notes

- Run audit lives in shared/run-audit.ts and profiler/run-audit.ts, with an optional runAudit field in ProfileInput/DatasetProfile. The existing utility-process parser, cancellation, budgets, snapshots and profile persistence are reused. `audit_run_log` is the trusted model tool; `auditRunLog` is the manual typed command. See README for the strict log format and limits. No arbitrary scoring code executes. Manual results stay in task checker history; only run-owned/preflight profiles enter that model report's Markdown.
- Final run-audit fixture: `C:\Users\nxan2\AppData\Local\Temp\orglet-run-audit-ui-3p1czT`, task `6945a4af-6e8f-4ffa-bd1a-ea62e6c5aa6e`. Source hashes and backup schema retain the audit; nested source/row counts are validated. `pnpm test:run-audit` is in CI. Only explicit RunAuditInputError messages are exposed; arbitrary parser errors remain generic.

- Agent Skills code lives in shared/skill-package.ts (bounded contracts), core/skill-package.ts (parser, hashes, capability checks, scoped resource access), main/skill-files.ts (native directory IO), and renderer/components/SkillReview.tsx (library/review). yaml 2.9.1 is a direct pinned dependency; upstream stable release and API were checked. `scripts/skill-smoke.mjs` is included in CI as `pnpm test:skills`.
- Local approval uses the `reviewedSkills` setting keyed by skill ID plus package hash. This setting is intentionally absent from backup settings and team templates. `reviewedHash` is only a workspace UI projection; Runner checks the local setting instead of trusting imported flags. Imported packages remain immutable; editing requires re-import. Resource reads consume the existing six-step/context/budget envelope and do not grant source evidence citations. Export adds the Orglet-specific manifest when absent, preserving all existing package files.
- Skill smoke final fixture: `C:\Users\nxan2\AppData\Local\Temp\orglet-skill-ui-phuiKg`. Native computer use fixture: `C:\Users\nxan2\AppData\Local\Temp\orglet-skill-ui-m13vCA`. All app windows and smoke/build processes from this milestone have closed.

- No paid model requests have been made. Do not search for an API key: the user will supply a file path later. Future OpenAI acceptance is capped at $0.05.
- Keep both E2E and native computer use for material UI flows, as requested. The native tests created a test worker, a Research Review team and demo history in the ordinary app data directory; preserve them.
- Git is initialized locally with no commits or remote. Do not publish or push without a request.
- Core state is owned by the utility process. Do not add a second SQLite writer or bypass the typed IPC bridge for new features.
- Next implementation targets: richer deterministic scoring/stability checks, followed by the remaining plan requirements (including reusable-knowledge review/tags and release gates). Live team budget changes, partial-report acceptance, accumulation of exclusions across multiple folder selections and transient preflight notices are fixed and covered by integration/E2E checks.
- Routines live in core/orchestration/routines.ts; time calculations and schemas are in shared/schedule.ts. Tick is called by the core every five seconds. First tick or a gap/overdue delay above 30 seconds skips to a future occurrence and retains one pending choice. Saving a schedule resets next due and pending state. Approval fingerprints team/worker/skill/model/pricing config; each dispatch verifies selected source bytes and checks the saved revision again. Occurrence advancement and task insertion share one transaction. No background OS job or automatic resume is installed. Policy: [docs/routines.md](routines.md). On reopen the UI shows a status notice and per-schedule missed-run copy with catch-up/dismiss; N=1, one `pending` per routine.
- WorkPolicy checks current team hours and concurrency at task start, plus hours at every new step/preflight checkpoint. Shift end pauses cooperatively and snapshots handoff from committed data; startup completes a missing handoff. The UI keeps manual resume, and budgets/reservations are still enforced by Runner. A team defaults to at most four active tasks, with at most two parallel roles per task; there is no workspace-wide provider concurrency pool yet.
- Team JSON templates live in core/storage/templates.ts. The main process owns the 2 MB file read and atomic export; internal import/export commands are not renderer-callable. Strict versioned schemas and reference validation precede one transaction creating fresh revision-1 team/worker/skill records. Provider choices are preserved, but keys, source access, tasks and consent are absent. Common bounded UTF-8/atomic text helpers in main/files.ts also serve backups and Markdown export.
- Backup code is in core/storage/backup.ts. Main owns file dialogs/read/write; the renderer cannot supply backup JSON or call internal backup commands. Imported newer revision history does not replace current worker content, and subsequent edits allocate above the highest saved revision to avoid collisions. Restores are refused while tasks or local checkers are active.
- Checkpoints live in core/storage/checkpoints.ts and contain model messages/read IDs/step/reply phase. They can contain source text, stay outside renderer IPC/backup, and are deleted when a report commits. Reservation creation and the dispatch marker share one SQLite transaction. Received replies are saved before tool execution; unresolved `requesting` checkpoints cannot resume. Local read tools may repeat if interrupted before their checkpoint commits. Leases are for the single local core; no distributed ownership or exactly-once provider guarantee is claimed.
- TeamRunner freezes queued role snapshots before the first async step, then binds committed join inputs before dispatch. Cancelled unused roles have records but no reservations/artifacts; the cancellation test now checks that actual dispatch invariant. Pause preserves unstarted roles without letting startup mark an already cancelled task interrupted.
- Preflight policy/record schemas are in shared/preflight.ts; execution is core/orchestration/preflight.ts. It verifies source hashes before and after checks, stores successful profile IDs, and completes before role dispatch. Final results are reused for the task; a permission/hash failure on reuse does not alter historical results. Profile IDs enter run snapshots, provider context and Markdown export. Backup format v1 accepts optional preflight records, with source/task/profile reference validation. Models still submit at most 30 limitations; the final artifact allows up to 100 so app coverage notices fit and are validated before commit.
- Native checker lives in `src/profiler`, launched by main in a disposable utility process. It receives selected-source copies and no credentials. The core remains the only SQLite writer. The 128 MB limit is DuckDB query memory, not a hard OS process cap; a whole-app crash may leave temporary copies.
- `pnpm typecheck` once stalled during package-manager housekeeping after adding DuckDB. Direct local CLI equivalents (`node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vitest/vitest.mjs run`, Forge CLI) worked. Do not restart live build jobs based only on silence.

## Design brief

Audience: a single Windows user delegating file review to reusable workers. Primary action: write a task, select permitted sources, run, and read an evidence-backed report. Familiar pattern: chat workspace, recent tasks in a compact sidebar, bottom composer, settings/details in a drawer. Monochrome system-font light/dark theme from the plan; character comes from the concise task-to-report interaction rather than decorative panels.

## Structured review milestone

Stored reports now optionally include structured checks (pass/fail/not_assessed), overall recommendation, draft feedback, frozen upstream finding references and unresolved conflicts. New tool schemas request this contract. Core and backup validation check references and prevent ready recommendations with empty/nonpassing checks, explicit conflicts or critical current findings. Historical replies/reports may omit review metadata; this compatibility path does not prove checklist coverage.

Renderer shows checks, recommendation, feedback and original conflicting findings; Markdown preserves review metadata. Previous exported skill report schemas remain supported. Tests: full suite 97/15 files passed; the six finding integration tests were then strengthened for review export/backup roundtrip and passed again. Final typecheck passed. Forge make completed. Extended packaged finding E2E passed recommendation/not-assessed/feedback plus source/checker navigation. Native computer use observed the missing-log message and feedback, then collapsed the check disclosure. Fixture was synthetic, not model output. All test windows/processes closed.

Remaining in this workstream: template-required check coverage and missing-log gating, persisted input requests, feedback-copy/revision actions, broader conflict UI fixtures, semantic conflict detection/evaluation and acceptance rules. Review references retain declared disagreements but cannot detect every semantic disagreement. Audit transitive upstream references and cyclic malicious backup graphs before claiming complete conflict integrity. No live provider or installed-Setup test occurred.

## Join integrity follow-up

Backup validation now checks the complete run-to-artifact dependency graph, including runs without committed reports. It rejects cycles, duplicate links and cross-task links before restore; iterative topological traversal avoids recursion depth limits. A synthesis cannot recommend ready while an upstream report contains a critical finding or an unresolved structured recommendation. Full suite: 99 tests across 15 files passed; final strict typecheck passed after correcting TypeScript control-flow narrowing. No UI changes in this follow-up. The packaged executable predates these core changes and must be rebuilt before the next packaged acceptance run.

## Required checklist milestone

Team.reviewPolicy is optional, versioned and preserved through snapshots, team editor saves, templates and backup. New Eris templates require five named checks. Synthesis fills omissions as not_assessed and sets insufficient_evidence. Run-stability PASS additionally needs a cited, run-scoped/preflight run-audit profile with observations status; this necessary gate does not prove statistical stability. Core is generic and renamed-team behavior is tested. Old teams are unchanged. Demo truthfully shows missing assessments.

Validation: full suite 102 tests/15 files passed; after tightening run-audit status to observations, 16 targeted structured-review/team tests passed. Typecheck passed. Forge make completed with current join-integrity and checklist changes. Packaged checker/template/backup E2E passed all five missing checks and preserved policy through team editing/export/import/restore. Native computer use opened Run stability on the restored Demo report and observed its missing-assessment explanation. All build/smoke processes and windows closed. Fixture: orglet-package-XH18t0; restored fixture: orglet-restored-GViA1W.

Next: persisted requests for additional evidence and review revisions; editable checklist controls in team settings; scoring checks; conflict UI/evaluation; knowledge/context compiler; optional adapter and release/live evaluation gates. Checklist names currently appear in English from the template. Automatic coverage notices still use the technical prefix Core and should be rewritten for users. No real API call occurred.

## Feedback copy action

Added copyFeedback to the typed preload bridge and authorized main IPC. Main requests feedbackText by artifact ID from core and writes only that persisted bounded text to Windows clipboard. Renderer exposes a copy button with busy/error/success feedback; historical reports without draft feedback return an explicit error. No renderer clipboard/read API is exposed.

Typecheck and six strengthened finding integration tests passed. Packaged finding E2E verified the IPC payload with a clipboard-write spy, restored the real write function, and passed existing evidence navigation. Native computer use clicked the real copy button, opened a new unsent composer, pasted, and visibly confirmed the exact fixture feedback. It did not submit the draft. Forge make completed; all test processes/windows closed. Latest full suite remains 102 tests from the checklist milestone.

## Checklist editor

Team settings now supports adding/removing up to 20 required checks, renaming them and choosing source-only or run-audit evidence. Blank/duplicate names fail before save and open the checklist. Saving creates a new revision. Removed technical Core prefixes from new missing-check notices. Typecheck and 16 targeted review/team tests passed. Packaged E2E passed rename/add/duplicate rejection/remove, run-audit selection preservation, team save, report coverage, template and backup roundtrip. The first E2E attempt timed out on an overly strict select-label locator; correcting that locator passed without changing behavior or assertions.

Native computer use opened the editor, renamed the first check and saved. A read-only query of the isolated database confirmed revision 3 had the new name while the task snapshot retained the old name. Forge make completed; all test windows/processes closed. Current packaged output is up to date. Latest full suite remains 102 tests; no live provider was used.

## Persistent evidence-request records

Final/standalone reports with not_assessed checks now atomically append a pending EvidenceRequest to Task, linked to the committed artifact. Requests survive backup; validation checks task/artifact ownership and exact missing-check names. acknowledgeEvidence is task/request scoped, refuses active work or stale acknowledgments, records an event and does not modify artifact checks/recommendation/acceptance. UI shows missing items and Ghi nhận giới hạn. Full suite 102 tests/15 files and final typecheck passed. Packaged checklist/template/backup E2E passed pending request preservation. Native computer use acknowledged the restored request and observed both success and unchanged insufficient-evidence status. Forge make completed; all associated test windows/processes closed.

This is only the persistence/acknowledgment portion of the requested input workflow. TaskStatus still has no waiting_for_input, no supplemental-source revision workflow exists, and acknowledgment is not a model rerun or resolution of missing evidence. Next implementation must preserve per-attempt sources, permission consent and immutable artifacts when adding sources/review feedback; do not mutate the old task source list without fixing source snapshot/export/backup contracts. EvidenceRequest.state currently supports pending/acknowledged only. No real API calls.

## Frozen run input

New runs snapshot brief, selected source IDs and excluded-source notices. Standalone/team creation freezes these before asynchronous dispatch, and direct Runner callers capture an input when absent. Runner uses that scope while live task consent/revocation and budgets still apply. Markdown exports use the run source list. Backup rejects finding/check sources outside the run input and snapshot sources outside the task. Legacy snapshots remain readable through task fallback; no fabricated historical capture claim.

104 tests across 15 files and final typecheck passed. Regression tests cover frozen provider brief/source manifest, scoped Markdown export, checksum-recomputed out-of-run citations and rejection before dispatch for out-of-task sources. This turn changed core contracts only; no UI flow was added. out/ still contains the previous evidence-request build and must be rebuilt before the next packaged validation. No active test/build processes remain. See .agents/plans/review-input-revisions.md for the next revision workflow work.

## Preflight scope versioning

Database schema v5 removes the one-preflight-per-task unique constraint transactionally, retaining all records and indexing task_id. Preflight reuse now matches policy, sorted source hashes and excluded-source count; differing selections/policies retain separate records. Legacy records remain readable. Backup permits multiple distinct scopes per task and rejects duplicate scopes.

Validation: typecheck passed. Full run had 105/106 tests pass; the sole failure was the old migration test expecting v4. After updating that expected target to v5, all eight checkpoint tests passed. The two new preflight tests passed in the full run: v4 data preservation and distinct-scope/reordered-source reuse with backup validation. No UI changes or packaged run this turn. out/ still predates both frozen-run-input and preflight-v5 changes; rebuild before packaged validation. This prepares revised evidence selection but does not yet expose a revise command or response composer.

## Input revision orchestration

Added reviseTask core command with explicit selected-source input, provider consent and total task budget. It retains the task history source union (bounded to 1,000 IDs), stores currentInput/inputRevision, resets acceptance and dispatches fresh roles. Team retry/resume reuse is restricted to the current inputRevision. Each revision binds to its matching preflight scope. Earlier snapshots lacking input are populated from the unchanged pre-revision task scope before expansion. The budget is cumulative for the same task, not a fresh per-revision allowance.

108 tests/15 files and typecheck passed. New tests verify new roles/join IDs, unchanged old artifacts, new selected-source preflight, source-scoped Markdown, cumulative usage, consent rejection without a revision write and backup validation. No UI revision composer exists yet, and prior report content is not automatically resent. out/ predates these core changes and needs rebuilding for the UI work. Next: response/revision composer with explicit consent, current-input display and prior-report navigation; test legacy backup merging after additive snapshot input capture before claiming full compatibility.

## Revision composer validation

The task view now opens a revision form with current source selection, file supplementation, editable brief, cumulative task budget and fresh provider consent. Packaged revision E2E passed with two revisions, eight artifacts, two preflight scopes and unchanged original artifacts. Native computer use submitted another Demo revision; persisted state confirmed revision 2 completed with twelve artifacts. Typecheck and Forge make passed; desktop and run-audit smoke passed after updating source-button labels. No live provider requests were made. Legacy snapshot-backfill backup compatibility and incomplete-revision report presentation remain open follow-ups.

## Legacy backup input scope

Restore compares legacy snapshots using their own backup task's original brief and sources, allowing the later additive RunInput backfill without weakening conflicts on actual snapshot content. Newly restored legacy runs get explicit input before the task/source merge. This prevents an expanded task history from broadening an old run's evidence scope. Nine backup integration tests and strict typecheck passed; packaged output needs rebuilding for this core change.

## Revision history and acceptance scope

TaskThread can select prior input revisions, showing their frozen brief and final report. The default uses the current revision only, avoiding fallback to an earlier synthesis. Historical reports retain export but do not expose acceptance. Core acceptance now verifies the report belongs to current inputRevision. Eight team tests, typecheck, Forge make and packaged revision E2E passed. Native computer use selected the first revision and observed original brief, history label and absent accept action. Follow-ups: selector polish, revision-scoped preflight navigation and waiting-for-input lifecycle.

## Local harness backends (2026-09-16, Claude)

User decision: connect installed agent harnesses (Claude Code, Codex, others present locally) first, with per-machine detection. Implemented `core/harness/detect.ts`, `core/harness/exec.ts`, provider IDs `claude-code`/`codex` with per-task consent, a shared `Runner.finalize` report gate, source-copy execution with cleanup, settings/worker/consent UI and `scripts/harness-smoke.mjs`. Validation: typecheck; 133 tests across 18 files; Forge make; packaged harness, desktop and knowledge smokes. Real detection found both CLIs here; both live probes stopped at authentication (Claude Code CLI logged out, Codex token expired), so no successful live harness review exists yet. Details and capability matrix: README "Local harnesses", `docs/capabilities.md`.

## Reviewed knowledge and context compiler (2026-09-15, Claude)

Schema v6 adds `knowledge`, immutable `knowledge_revisions` and FTS5 `knowledge_search`. Items are scoped to workspace, team or worker and are proposed, approved or archived; every save or decision is a new revision. Models may propose up to three items via `submit_report.knowledgeProposals`; they are stored as proposals with run/artifact provenance. Team templates carry only that team's approved items and import them as proposals. Backups carry items and revision history and validate scope and provenance references.

`core/context/compiler.ts` builds platform → team → worker → skill → approved in-scope knowledge, removes normalized duplicates (templates reuse the same text for team instructions and skill), ranks pinned then keyword overlap with the brief, and bounds knowledge at 12 items / 16 KB. Runner freezes `run.snapshot.context` (loaded revisions, hashes, omissions with reason) before the first dispatch, including Demo runs. UI: Library tabs Skills/Knowledge with search, review, edit and archive; pending badge; per-task proposal links; context manifest in Chi tiết.

Other changes in the same session: workspace-wide provider request concurrency (setting 1–4, default 2; queued steps hold no reservation), two-file comparison of column names, row counts and ID set differences plus a `pair_alignment` required-check gate (new Eris templates use it for submission/answer alignment), line-level finding locations validated against the re-read source and highlighted in the source preview, pre-upgrade `VACUUM INTO` database copies with `docs/recovery.md`, revision history UI polish (styled selector, preflight button scoped to the viewed revision, current-task status hidden in history), `docs/release-review.md`, and the Codex/quota status table in `docs/capabilities.md`.

Validation: strict typecheck and 125 tests across 17 files passed. Forge make produced the package, ZIP and Squirrel Setup. Packaged smokes passed on the final build: knowledge (new), desktop, finding (with line highlight), run-audit; on the previous build of the same session: packaged checker/template/backup, revision, skill, routine. The first run-audit attempt timed out while another smoke chain ran in parallel and passed alone. Native computer use was not performed: access to the Orglet window was declined in this session. No live provider calls.

## Waiting for evidence

Tasks now persist waiting_input after final reports with not_assessed checks. Runs remain completed. Acknowledgment preserves the waiting state; explicit acceptance completes the task without changing report content, while supplementation creates a new revision. Routines defer while the previous task waits. Typecheck passed; full suite had 110/111 passing with one superseded completion expectation, then all 18 team/preflight tests passed after updating it. Packaged revision E2E passed with acknowledgment and supplementation assertions. Native waiting-state and restart/backup-specific checks remain follow-ups.

## Interface language

User decision 2026-09-17: the interface is available in Vietnamese (default), English (US) and English (UK), chosen under Settings → General → Language and stored in the workspace settings. Vietnamese source text is the translation key: renderer strings are wrapped in `t('…')` (placeholders `{0}`), English lives in `apps/desktop/src/shared/locales/en.ts`, and British English is derived from it by a short spelling list. The English dictionary is a separate renderer chunk loaded the first time English is chosen. Core and main-process messages stay Vietnamese in code and are translated when shown (`tMessage`, which also matches messages with values filled in); native file dialogs use the stored language. `node scripts/i18n-keys.cjs --unused` lists missing or unused entries; `scripts/i18n-wrap.cjs` wraps new Vietnamese text. Validation: `tests/integration/i18n.test.ts` (placeholders, message patterns, UK spellings) and `scripts/i18n-smoke.mjs` (switching, persistence, validation text).

## Team chat shell (COD-24)

Clicking a team opens that team's chat (roster in the sidebar and header). One live `tasks` row keyed by `teamId`; the first message is `createTask`, later messages `reviseTask`. Worker chat and group (`assignees`) chat are unchanged. Docs: [team-chat.md](team-chat.md), policy [team-chat-context.md](team-chat-context.md).

## Team orchestrator (COD-25)

A team-chat turn runs plan → assigned members → one synthesis report (`TeamRunner.run`). Plan is a `runs` row (`stage: 'plan'`) with `snapshot.plan`; it is not a user-facing artifact. Unassigned members are cancelled with a named skip. A failed plan does not dispatch members or invent results. Partial member failure stays `partial` with named `Role chưa hoàn tất` limitations. Cancel aborts the whole turn; retry reuses a completed plan and unfinished jobs only. Tests: `tests/integration/team.test.ts`.

## Hide task pile (COD-26)

The sidebar is **Nhóm** and **Nhân viên**, not a **Công việc** list. Click a worker or a team to open the live thread (`liveWorkerTask` / `liveTeamTask`). Empty composer find-or-creates; later sends `reviseTask`. Archive from the thread ⋯ menu to start over. Search still finds older chats. **Chi tiết** keeps jobs, dollars, retry and cancel. Routines stay on **Lịch chạy**. Docs: [team-chat.md](team-chat.md).

## Model list fetch and cache (COD-31)

Core command `modelList({ provider, refresh? })` loads each connection's models from that provider's own API or CLI, stores them in `settings.modelLists` (24h TTL, stale-while-revalidate, excluded from backup), and always sets `customIdOk`. OpenAI `shutdown_date` and Codex `upgrade` are stored for the picker chip. Tests: `tests/integration/model-list.test.ts`. Plan: [model-list-fetch.md](model-list-fetch.md).

## Worker model picker (COD-28)

Workers store optional `modelId`. The worker dialog lists the cached models for that connection and always accepts a typed ID. Catalog defaults are suggestions. Native adapters and harness CLIs (`--model` / Codex `-m`) use the saved ID. Custom OpenAI/Anthropic IDs are not billed at mini/Haiku rates. Tests: `tests/integration/worker-model.test.ts`.

## Deprecated model chip (COD-30)

The model picker chips a selected or suggested ID when the cached list has `deprecated: true`. A sunset day is shown only from native `sunsetAt` (OpenAI `shutdown_date`). Anthropic, xAI and harness lists omit dates; Orglet does not scrape HTML or guess them. Codex `replacementId` is a quiet “prefer” line, not an automatic switch. Opening the dialog does not toast. Tests: `tests/integration/model-deprecation.test.ts`.
