// Moved into the kit (COD-274); the app passes its own label and icon where it uses the bar.
export { ReactionBadges, ReactionBar, ReactionPicker } from '@codepawl/orglet-ui';
export type { ReactionBadge, ReactionOption } from '@codepawl/orglet-ui';
